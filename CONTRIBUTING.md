# 奉天1928 - 贡献指南

## 欢迎参与

感谢你对《奉天1928》项目感兴趣！无论你是经验丰富的写手还是第一次尝试小说创作的爱好者，你的参与都至关重要。在阅读本指南之前，请先阅读 CODE_OF_CONDUCT.md，了解我们的行为准则。

## 写作协作流程

### 1. 认领章节

- 查看 Issue 列表，寻找标注为 “open” 的章节认领任务
- 也可以在 Issue 中留言表示想写某个章节，项目维护者会进行分配
- 认领后请在 Issue 下回复确认，避免多人同时写同一章节

### 2. 分支工作流

```
main (主分支，存放审校通过的定稿)
  └── feature/章节编号-章节标题 (个人写作分支)
```

- 从 main 分支创建你的写作分支：`feature/001-救父开篇`
- 在分支中进行创作，频繁提交 commit 记录进度
- 完成后提交 Pull Request 请求审校合并

### 3. 文件命名规范

章节文件统一放在 `chapters/` 目录下，命名格式：

```
chapters/
  卷一/
    001-救父开篇.md
    002-暗流涌动.md
  卷二/
    003-远赴德国.md
```

- 使用中文标题，简洁明了
- 文件名前加三位数序号，保证排序正确
- 使用 `.md` (Markdown) 格式编写

### 4. 写作格式要求

- **标题层级**：使用 Markdown 标准标题语法（#、##、###）
- **章节标题**：每章开头第一行为章节标题（## 级别）
- **正文**：直接写正文内容，不要额外加格式标记
- **对话**：直接书写对话内容，可使用引号标注
- **场景切换**：用空行或 `---` 分隔符区分场景
- **字数建议**：每章 3000-5000 字为宜
- **标点符号**：遵循 GB/T 15834—2011《标点符号用法》：中文文本使用全角标点，对话用弯引号“”（嵌套用‘’），禁用半角引号/半角标点与中文混排（数字内部如 1,500、2.5 除外）

### 5. 文风一致性要求

- **叙事视角**：以第三人称限知视角为主，可偶尔切换至张学良内心独白
- **语言风格**：半文半白，既有民国时代感又便于现代读者阅读
- **人物称呼**：正式场合用全称（如“张学良”），亲密场景可用小名（如“汉卿”）
- **禁用词**：避免使用现代网络用语、港台用语混入民国背景
- **史实与虚构**：真实历史人物按史实行事，虚构情节需标注 [虚构]

### 6. 审校流程

1. 提交 PR 后，由指定审校员（见项目成员列表）进行审校
2. 审校重点：
   - 历史事实是否合理（不违背基本史实逻辑）
   - 人物性格是否前后一致
   - 文风是否符合项目规范
   - 情节逻辑是否自洽
3. 审校意见以 PR Comments 形式提出
4. 作者根据意见修改后再次提交
5. 审校通过后由维护者合并至 main 分支

### 7. 冲突解决机制

- 对同一章节有不同创作想法时，先在 Issue 中讨论达成共识
- 如无法达成一致，由项目发起人/主编最终裁定
- 鼓励保留不同版本的创作在分支中供参考，不强制删除

## 8. 备份保留规则

为控制仓库体积、避免备份堆积造成版本混乱，备份遵循「每单位仅留最新 1 份 + 日期戳」原则：

- **单一可信源**：写作以 `chapters/`、`worldbuilding/OUTLINE/` 等分卷/分幕正稿为权威；备份仅为快照，不作为协作源。
- **保留数量**：每个写作单位（一卷 / 一幕 / 一份大纲文档）**仅保留 1 份最新备份**。
- **命名**：保留的备份文件名带快照日期戳，格式 `原名.bak-YYYY-MM-DD`（或 `_backupN_YYYY-MM-DD`）。日期戳取该草稿达到该状态的日期（文件修改时间）。
- **移除前必记录**：移除旧备份前，须将其相对最新备份的差异（修改点）记入 CHANGELOG，作为可追溯的修改项；不得无记录直接丢弃。
- **备份头标注**：保留的最新备份文件头部以 HTML 注释标注快照日期、保留依据与修改点索引（指向 `CHANGELOG.md` 对应条目）。
- **生成新备份时**：若同单位已存在带日期戳的旧备份且内容已被新正稿覆盖，直接移除旧备份（其差异已可追溯到 `CHANGELOG.md` 或正稿历史）。

> 修改记录位置：项目根 `CHANGELOG.md`（2026-09-26 治理时的原始记录《备份修改记录》已归档至 `_trash/备份修改记录.md`）

## 9. 文档权威层级（草稿=工作源、定稿目录=权威）

为避免“草稿 ↔ 定稿”多处漂移，文档分两级管理：

| 内容域 | 工作草稿（工作源） | 定稿目录（权威） |
|--------|-------------------|------------------|
| 世界观 | `notes-personal/世界观草稿.md` | `worldbuilding/`（各体系文档） |
| 角色 | `notes-personal/角色设定草稿.md` | `characters/`（各角色档案） |
| 大纲 | `notes-personal/大纲草稿.md` | `worldbuilding/OUTLINE/`（分卷大纲） |

- **冲突裁定**：草稿与定稿不一致时，**一律以定稿目录为准**。
- **修改流向**：设定/大纲变更**先改定稿目录，再同步回对应草稿**；禁止只改其一。
- **写作取材**：正文写作（`chapters/`）取材以定稿目录为唯一事实来源，草稿仅作过程参考。
- **漂移自检**：每次大改动后，对三组“草稿↔定稿”做关键词抽查（兵力口径、年份锚点、红线表述等），确认无残留旧表述。

## 世界观与设定文档

写作前务必阅读以下核心文档，确保设定一致：

- [synopsis.md](synopsis.md) — 故事大纲
- [characters.md](characters.md) — 角色设定
- [worldbuilding.md](worldbuilding.md) — 世界观设定
- [style-guide.md](style-guide.md) — 文风指南

## 提交规范

### Commit 信息格式

```
[章节号] 章节标题: 简要说明修改内容

例如：
[001] 救父开篇: 完成第一章初稿，约4500字
[002] 暗流涌动: 修改第三章人物对话部分
```

### PR 描述模板

```
## 本 PR 包含
- 新增/修改的章节：第X章 XXX
- 修改内容概述

## 审校关注点
- 历史事实核查
- 人物一致性
- 文风统一性
```

## 有问题？

欢迎通过 GitHub Issues 提问或讨论，我们会尽力及时回复。感谢你的贡献！
